{
  "name": "rag-platform-widget",
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "build": "esbuild widget.ts --bundle --minify --outfile=dist/widget.js --target=es2020",
    "build:dev": "esbuild widget.ts --bundle --outfile=dist/widget.js --target=es2020 --sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
