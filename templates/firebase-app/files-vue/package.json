{
  "name": "{{projectName}}",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "stacksolo dev",
    "build": "npm run build:web && npm run build:api",
    "build:web": "cd apps/web && npm run build",
    "build:api": "cd functions/api && npm run build",
    "install:all": "npm install && cd apps/web && npm install && cd ../../functions/api && npm install"
  },
  "devDependencies": {
    "@stacksolo/cli": "^0.1.0"
  }
}