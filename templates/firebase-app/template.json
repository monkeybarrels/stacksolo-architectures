{
  "id": "firebase-app",
  "name": "Firebase App",
  "description": "Full-stack app with Firebase Authentication and Firestore database. Includes login/signup UI, protected routes, and authenticated API endpoints.",
  "variables": [
    {
      "name": "projectName",
      "description": "Your project name (lowercase, hyphens only)",
      "type": "string",
      "required": true
    },
    {
      "name": "gcpProjectId",
      "description": "Your GCP project ID",
      "type": "string",
      "required": true
    },
    {
      "name": "region",
      "description": "GCP region for deployment",
      "type": "string",
      "default": "us-central1"
    }
  ],
  "variants": [
    {
      "id": "react",
      "name": "React",
      "description": "React with Vite, TypeScript, and React Router",
      "filesDir": "files-react"
    },
    {
      "id": "vue",
      "name": "Vue",
      "description": "Vue 3 with Vite, TypeScript, and Vue Router",
      "filesDir": "files-vue"
    }
  ],
  "dependencies": [
    "@stacksolo/runtime"
  ]
}