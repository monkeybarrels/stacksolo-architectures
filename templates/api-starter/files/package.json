{
  "name": "{{projectName}}",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd functions/api && npm run dev",
    "build": "cd functions/api && npm run build",
    "install:all": "npm install && cd functions/api && npm install"
  },
  "devDependencies": {
    "@stacksolo/cli": "^0.1.0"
  }
}